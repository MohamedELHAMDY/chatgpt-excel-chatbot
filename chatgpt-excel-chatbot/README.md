# ChatGPT Excel Chatbot

This project allows you to interact with an Excel file through a chatbot powered by OpenAI's GPT-3.

## Requirements:
- Python 3
- `openai`, `pandas`, `openpyxl`

## How to Run:
1. Install dependencies:
```bash
pip install -r requirements.txt
